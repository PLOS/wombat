{
  "name": "wombat-frontend",
  "version": "1.0.0",
  "description": "Wombat Frontend assets pipeline",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "PLOS",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-if": "^2.0.2",
    "gulp-sass": "^3.0.0",
    "gulp-uglify": "^3.0.1"
  }
}
