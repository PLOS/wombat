<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xmlns:aop="http://www.springframework.org/schema/aop"
             xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/aop
		http://www.springframework.org/schema/aop/spring-aop-3.0.xsd">

  <!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->

  <context:component-scan base-package="org.ambraproject.wombat.controller"/>

  <beans:bean class="org.ambraproject.wombat.config.SpringConfiguration" />

  <!-- Define a default controller that handles all requests not matched by any other
       controller.  This controller serves the 404 page.  We have to do it this way,
       instead of using the error-page directive in web.xml, because we want to preserve
       any site path variable present so we can render the 404 page with site-specific styling.  -->
  <beans:bean class="org.springframework.web.servlet.mvc.annotation.DefaultAnnotationHandlerMapping">
    <beans:property name="defaultHandler" ref="notFoundController"/>
  </beans:bean>

  <!-- Enables the Spring MVC @Controller programming model -->
  <annotation-driven>
    <argument-resolvers>
      <beans:bean class="org.ambraproject.wombat.controller.SiteResolver" />
    </argument-resolvers>
  </annotation-driven>

  <!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
  <resources mapping="/resources/**" location="/resources/"/>

  <!-- Performance monitoring  -->
  <aop:aspectj-autoproxy proxy-target-class="true"/>

  <beans:bean id="performanceMonitor"
        class="org.springframework.aop.interceptor.PerformanceMonitorInterceptor" />

  <aop:config>
    <aop:pointcut id="allWombatMethods" expression="execution(* org.ambraproject.wombat.*.*.*(..)) &amp;&amp;
      !execution(* org.ambraproject.wombat.config.*.*(..))"/>
    <aop:advisor pointcut-ref="allWombatMethods" advice-ref="performanceMonitor" order="1"/>
  </aop:config>

  <aop:config>
    <aop:pointcut id="springHandlerMethods" expression="execution(* org.springframework.web.servlet.handler.AbstractHandlerMethodMapping.*(..))"/>
    <aop:advisor pointcut-ref="springHandlerMethods" advice-ref="performanceMonitor" order="2"/>
  </aop:config>

  <!--<beans:bean id="customizableTraceInterceptor"-->
              <!--class="org.springframework.aop.interceptor.CustomizableTraceInterceptor">-->
    <!--<beans:property name="enterMessage" value="Entering $[methodName]($[arguments])"/>-->
    <!--<beans:property name="exitMessage" value="Leaving $[methodName](): $[returnValue]"/>-->
  <!--</beans:bean>-->

  <!--<aop:config>-->
    <!--<aop:pointcut id="allMethods" expression="execution(* org.ambraproject.wombat.*.*.*(..))"/>-->
    <!--<aop:advisor advice-ref="customizableTraceInterceptor"-->
                 <!--pointcut-ref="allMethods"/>-->
  <!--</aop:config>-->

</beans:beans>
