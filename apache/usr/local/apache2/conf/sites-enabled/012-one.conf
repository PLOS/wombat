<VirtualHost *:80>
  ServerAdmin admin@plos.org
  DocumentRoot /var/www/ambra
  ServerName www.plosone.org
  ServerAlias plosone plosone.org plosone.com www.plosone.com plos1.org www.plos1.org one.plos.org plosone.plos.org one.plosjournals.org one.plosjournals.com plosone-proxy.plos.org
  AllowEncodedSlashes On

  <Directory /var/www/ambra/>
    Order Allow,Deny
    Allow from all
  </Directory>

  <Location /admin>
    Order Deny,Allow
    Deny from all
  </Location>


  ErrorDocument 503 /journals/PLoSONE/webapp/503.html

  # Strip terminal jsessionid (or naked semicolon which can appear for an unknown reason)
  RewriteRule (.*);(jsessionid=[^/]+)?$      $1 [R,L]

  Include conf/rewrite.d/ambra.conf

   ## (everything else, serve up directly)

  RewriteCond %{QUERY_STRING}              issue=info.*issue.pone.c01.i(..)

  SetEnvIf User-Agent "KEMP.*" nolog
  SetEnvIf User-Agent "check_http.*" nolog
  CustomLog "|/usr/bin/logger -p local6.info -t journals" lb_vhost_combined env=!nolog
  CustomLog "|/usr/bin/cronolog /var/log/apache2/combined-%Y%m%d-access_log" lb_combined env=!nolog
</VirtualHost>
